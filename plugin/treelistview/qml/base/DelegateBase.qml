import treelistview 1.0
import "../private"

InputHandler {
    //TODO use classTreeNodeProperties instead set of properties (as in RowBackgroundDelegateBase)
    property var index: __index
    property var modelData: __modelData
    property var view: __view
}
